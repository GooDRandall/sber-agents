---
alwaysApply: true
---

# Workflow для разработки бота (KISS)

Основан на `docs/vision.md` и плане `docs/tasklist.md`. Делай только необходимое для MVP.

## Принципы
- Строго следуем шагам из `docs/tasklist.md`, без прыжков вперёд.
- Перед реализацией каждого шага — короткое согласование решения с примерами кода.
- Малые инкременты, читаемый код, без оверинжиниринга.

## Итеративный цикл (для каждой итерации из tasklist)
1) Подготовка
   - Обозначь итерацию (номер, цель).
   - Предложи минимальное решение: краткое описание + ключевые фрагменты кода (файлы/функции), без полной реализации.
   - Дождись согласования.
2) Реализация
   - Реализуй согласованные изменения.
   - Минимально протестируй вручную (как указано в итерации).
3) Обновление прогресса
   - В `docs/tasklist.md`:
     - В таблице «Прогресс» для итерации поставь статус: 🚧 при старте, ✅ после завершения.
     - В чекбоксах задач — отметь выполненные `[x]`.
   - Коротко дополни «Комментарий» (1–2 слова при необходимости).
4) Подтверждение
   - Покажи ключевые изменения (списком файлов + краткая суть).
   - Запроси подтверждение на переход к следующей итерации.

## Согласование решения (перед реализацией)
- Дай краткую формулировку, что и зачем делаем в этой итерации.
- Приложи короткие отрывки кода (цитаты 5–15 строк) для ключевых мест.
- Укажи, как это протестировать (минимальные шаги).

## Обновление статусов
- Статусы в таблице прогресса: ⏳ — в плане, 🚧 — в работе, ✅ — готово, ❌ — отменено.
- Меняй статус только своей текущей итерации.

## Ограничения
- Не меняй `docs/vision.md` без явной договорённости.
- Не добавляй дополнительных задач и зависимостей без согласования.
- Не оставляй сломанное состояние; сдавай только рабочие инкременты после согласования.


