# Рекомендации для увеличения Answer Correctness

## Что такое Answer Correctness?

`answer_correctness` - это метрика RAGAS, которая оценивает правильность ответа относительно ground truth (эталонного ответа). Она комбинирует:
- **Факт-чекинг** - проверка фактической точности информации
- **Семантическую похожесть** - соответствие смысла ответа эталону

Значение от 0.0 до 1.0 (выше = лучше).

## ⚠️ Важно: Context Recall всегда = 1.0

### Что означает Context Recall?

`context_recall` - это метрика RAGAS, которая оценивает **полноту контекста**: содержат ли retrieved документы всю информацию, необходимую для правильного ответа (ground truth).

**Формула:** 
```
context_recall = (информация из ground truth, найденная в retrieved контексте) / (вся информация из ground truth)
```

### Почему Context Recall всегда = 1.0?

Если у вас `context_recall` всегда равен 1.0, это **нормально** для синтезированных датасетов, но указывает на проблему "data leakage" в evaluation:

**Причина:**
1. Ground truth создается из тех же чанков, которые индексируются в векторное хранилище
2. При evaluation система находит те же чанки через retrieval
3. Поэтому retrieved контекст всегда содержит всю информацию из ground truth
4. Результат: `context_recall = 1.0` для всех примеров

**Где это происходит:**
- В `src/dataset_synthesizer.py`, строка 193: `"contexts": [chunk.page_content]`
- Ground truth создается из чанка → чанк индексируется → retrieval находит этот чанк → context_recall = 1.0

### Это проблема?

**Для синтезированных датасетов - это ожидаемо**, но означает:
- ✅ Метрика не показывает реальное качество retrieval
- ✅ Нельзя оценить, насколько хорошо система находит информацию
- ✅ Все внимание на другие метрики: `answer_correctness`, `faithfulness`, `context_precision`

### Как получить более реалистичный Context Recall?

#### Вариант 1: Использовать независимые ground truth (рекомендуется)

Создавать ground truth из **других источников**, не из индексированных документов:
- Экспертные ответы
- Официальные FAQ
- Внешние источники знаний

#### Вариант 2: Сложные вопросы, требующие несколько чанков

Создавать вопросы, на которые нужно ответить, используя информацию из **нескольких чанков**:
- Вопросы, требующие агрегации данных
- Вопросы, требующие сравнения информации из разных частей документа
- Вопросы, требующие логического вывода

**Пример улучшенного синтеза:**
```python
# Вместо простого вопроса из одного чанка
"Какая процентная ставка по вкладу Сохраняй?"

# Создавать сложные вопросы
"Сравните условия вкладов Сохраняй и Пополняй. В чем разница в процентных ставках и минимальных суммах?"
```

#### Вариант 3: Использовать только JSON Q&A пары

Если у вас есть готовые Q&A пары из JSON, которые созданы независимо:
- Они могут иметь более реалистичный context_recall
- Но только если они не были созданы из тех же чанков

### Что делать сейчас?

1. **Принять, что context_recall = 1.0** - это нормально для синтезированных датасетов
2. **Фокусироваться на других метриках:**
   - `answer_correctness` - правильность ответа
   - `faithfulness` - обоснованность (нет галлюцинаций)
   - `context_precision` - точность поиска (релевантность найденных документов)
3. **Для более реалистичной оценки** - использовать независимые ground truth или создавать более сложные вопросы

### Связь с Answer Correctness

`context_recall = 1.0` не означает, что `answer_correctness` будет высоким:
- Даже если контекст полный, LLM может неправильно интерпретировать информацию
- LLM может добавить галлюцинации (низкий `faithfulness`)
- LLM может дать неточный ответ (низкий `answer_correctness`)

Поэтому `answer_correctness` остается важной метрикой даже при `context_recall = 1.0`.

## 🎯 Приоритетные рекомендации

### 1. Улучшение Retrieval качества (высокий приоритет)

**Проблема:** Если система не находит релевантные документы, ответ не может быть правильным.

**Решения:**

#### A. Использовать Hybrid + Reranker режим
```bash
# В .env
RETRIEVAL_MODE=hybrid_reranker
SEMANTIC_RETRIEVER_K=15  # Увеличить с 10
BM25_RETRIEVER_K=15      # Увеличить с 10
RERANKER_TOP_K=5         # Увеличить с 3
CROSS_ENCODER_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1
```

**Почему это помогает:**
- Hybrid режим находит документы и по смыслу, и по точным терминам
- Reranker переранжирует результаты для максимальной релевантности
- Увеличение k даёт больше контекста для LLM

#### B. Настроить веса Ensemble
```bash
# Если вопросы содержат много терминов (номера, названия)
ENSEMBLE_SEMANTIC_WEIGHT=0.4
ENSEMBLE_BM25_WEIGHT=0.6

# Если вопросы более абстрактные
ENSEMBLE_SEMANTIC_WEIGHT=0.6
ENSEMBLE_BM25_WEIGHT=0.4
```

#### C. Использовать более качественные embeddings
```bash
# Для лучшего качества (если доступен OpenAI API)
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-large

# Для локального использования
EMBEDDING_PROVIDER=huggingface
HUGGINGFACE_EMBEDDING_MODEL=intfloat/multilingual-e5-large  # Вместо base
```

### 2. Улучшение промпта для генерации ответов (высокий приоритет)

**Проблема:** Промпт влияет на то, как LLM интерпретирует контекст и генерирует ответ.

**Текущий промпт** (`prompts/conversation_system.txt`):
```
Ты ассистент Сбербанка для ответов на вопросы. Отвечай на вопросы пользователей 
на основе истории диалога и контекста, полученного для последнего вопроса. 

Если в контексте нет информации для ответа, строго отвечай: 
"Я не нашел ответа на ваш вопрос в доступных документах."

Используй максимум 3-4 предложения и давай конкретные ответы.
```

**Рекомендуемые улучшения:**

1. **Добавить инструкции по точности:**
```
Ты ассистент Сбербанка для ответов на вопросы. Отвечай на вопросы пользователей 
на основе истории диалога и контекста, полученного для последнего вопроса.

ВАЖНО:
- Отвечай ТОЛЬКО на основе информации из предоставленного контекста
- Если информация отсутствует в контексте, строго отвечай: 
  "Я не нашел ответа на ваш вопрос в доступных документах."
- Будь максимально точным и конкретным в фактах (цифры, даты, названия)
- Не добавляй информацию, которой нет в контексте
- Если в контексте есть несколько вариантов, упомяни все релевантные

Используй максимум 3-4 предложения и давай конкретные ответы.
```

2. **Добавить структурирование ответа:**
```
Ты ассистент Сбербанка для ответов на вопросы. Отвечай на вопросы пользователей 
на основе истории диалога и контекста, полученного для последнего вопроса.

ИНСТРУКЦИИ:
1. Внимательно прочитай вопрос и контекст
2. Найди в контексте информацию, которая отвечает на вопрос
3. Сформулируй точный ответ, используя ТОЛЬКО информацию из контекста
4. Если информации нет, ответь: "Я не нашел ответа на ваш вопрос в доступных документах."

ФОРМАТ ОТВЕТА:
- Будь конкретным (используй точные цифры, даты, названия из контекста)
- Структурируй ответ логично
- Используй максимум 3-4 предложения
```

### 3. Снижение Temperature LLM (средний приоритет)

**Проблема:** Высокий temperature (0.9) делает ответы менее детерминированными и может приводить к неточностям.

**Решение:**
```bash
# В src/rag.py, функция _get_llm()
# Изменить temperature с 0.9 на 0.3-0.5
temperature=0.3  # Для более детерминированных ответов
```

**Компромисс:** Ответы станут более предсказуемыми, но могут быть менее "естественными".

### 4. Улучшение Query Transformation (средний приоритет)

**Проблема:** Плохая трансформация запроса приводит к нерелевантному поиску.

**Текущий промпт** (`prompts/query_transform.txt`):
```
Преобразуй последнее сообщение пользователя в поисковый запрос на русском языке, 
учитывая всю историю диалога выше. Тщательно проанализируй все сообщения для 
создания максимально релевантного запроса. Запрос должен быть подробным, а не кратким. 
Лучше более абстрактный, чем конкретный. Ответь только поисковым запросом, ничего больше.
```

**Рекомендуемое улучшение:**
```
Преобразуй последнее сообщение пользователя в поисковый запрос на русском языке, 
учитывая всю историю диалога выше.

ИНСТРУКЦИИ:
1. Проанализируй весь диалог для понимания контекста
2. Извлеки ключевые термины и понятия из вопроса
3. Сохрани важные детали (номера, названия, даты)
4. Создай поисковый запрос, который поможет найти релевантную информацию

ПРИМЕРЫ:
- "Какие проценты по вкладу Сохраняй?" → "процентная ставка вклад Сохраняй условия"
- "А сколько минимальная сумма?" → "минимальная сумма вклад условия"

Ответь только поисковым запросом, ничего больше.
```

**Также можно снизить temperature для query transform:**
```python
# В src/rag.py, функция _get_llm_query_transform()
temperature=0.2  # Вместо 0.4 для более точной трансформации
```

### 5. Улучшение стратегии чанкинга (средний приоритет)

**Проблема:** Слишком маленькие чанки (500 символов) могут разрывать связанную информацию.

**Текущие настройки** (`src/indexer.py`):
```python
chunk_size=500
chunk_overlap=50
```

**Рекомендации:**

1. **Увеличить размер чанков:**
```python
# В src/indexer.py, функция split_documents()
chunk_size=800-1000  # Вместо 500
chunk_overlap=100-150  # Вместо 50
```

2. **Использовать семантический чанкинг** (если доступен):
```python
# Использовать SemanticChunker вместо RecursiveCharacterTextSplitter
from langchain_experimental.text_splitter import SemanticChunker
from langchain_openai import OpenAIEmbeddings

text_splitter = SemanticChunker(
    embeddings=create_embeddings(),
    breakpoint_threshold_type="percentile"  # или "standard_deviation"
)
```

**Компромисс:** Большие чанки дают больше контекста, но могут включать нерелевантную информацию.

### 6. Использование более мощной LLM модели (низкий приоритет, если доступно)

**Проблема:** Более слабые модели могут хуже понимать контекст и генерировать неточные ответы.

**Рекомендации:**
```bash
# Использовать более мощную модель для генерации ответов
MODEL=gpt-4o  # Вместо gpt-3.5-turbo или более слабых моделей

# Для query transformation можно использовать ту же модель или более легкую
MODEL_QUERY_TRANSFORM=gpt-4o-mini  # Или gpt-4o для лучшего качества
```

**Компромисс:** Увеличение стоимости и latency.

### 7. Увеличение количества контекста (низкий приоритет)

**Проблема:** Мало контекста может не содержать всю необходимую информацию.

**Решение:**
```bash
# Увеличить k для retriever
SEMANTIC_RETRIEVER_K=15-20  # Вместо 10
BM25_RETRIEVER_K=15-20      # Вместо 10
RERANKER_TOP_K=5-7          # Вместо 3
```

**Компромисс:** Больше контекста = больше токенов = выше стоимость и latency.

## 📊 Метрики, связанные с Answer Correctness

Улучшение `answer_correctness` также улучшит связанные метрики:

1. **Context Recall** - полнота контекста (влияет на correctness)
2. **Context Precision** - точность поиска (влияет на correctness)
3. **Faithfulness** - обоснованность (влияет на correctness)

## 🔄 Процесс оптимизации

### Шаг 1: Базовые измерения
1. Запустите evaluation: `/evaluate_dataset` в Telegram
2. Запишите текущие значения метрик
3. Определите проблемные области

### Шаг 2: Приоритетные улучшения
1. Переключитесь на `hybrid_reranker` режим
2. Улучшите промпт для генерации ответов
3. Снизьте temperature до 0.3-0.5

### Шаг 3: Измерение результатов
1. Запустите evaluation снова
2. Сравните метрики до/после
3. Определите, какие изменения дали наибольший эффект

### Шаг 4: Дополнительные оптимизации
1. Настройте веса ensemble под ваши данные
2. Улучшите query transformation
3. Оптимизируйте чанкинг

### Шаг 5: Финальная настройка
1. Увеличьте k для retriever (если нужно)
2. Используйте более мощную модель (если доступно)
3. Проведите финальное evaluation

## ⚠️ Важные замечания

1. **Не все изменения дают одинаковый эффект:**
   - Улучшение retrieval обычно даёт наибольший прирост
   - Улучшение промпта также критично
   - Остальные изменения - инкрементальные

2. **Компромиссы:**
   - Больше контекста = выше стоимость
   - Более мощная модель = выше стоимость и latency
   - Более детерминированные ответы = менее "естественные"

3. **Зависимости:**
   - `answer_correctness` сильно зависит от `context_recall` и `context_precision`
   - Если retrieval плохой, улучшение промпта не поможет

4. **Тестирование:**
   - Всегда тестируйте изменения на реальном датасете
   - Сравнивайте метрики до/после
   - Не оптимизируйте только одну метрику в ущерб другим

## 📈 Ожидаемые результаты

При правильной оптимизации можно ожидать:

- **Базовый уровень (semantic, temperature=0.9):** 0.6-0.7
- **После hybrid_reranker:** +0.05-0.10
- **После улучшения промпта:** +0.05-0.10
- **После снижения temperature:** +0.02-0.05
- **После всех оптимизаций:** 0.80-0.90+

## 🔗 Связанные документы

- [README.md](../README.md) - общая документация проекта
- [docs/vision.md](vision.md) - описание метрик RAGAS
- [docs/references/rag-evaluation-practice.ipynb](references/rag-evaluation-practice.ipynb) - референсный ноутбук

