# Отчёт о выполнении задания

## Название проекта и краткое описание

**RAG-ассистент Сбербанка**

Telegram-бот с RAG (Retrieval-Augmented Generation) для ответов на вопросы по документам Сбербанка о кредитах и вкладах. Система использует LangChain для реализации RAG-цепочки, автоматически индексирует PDF документы и JSON файлы с Q&A парами, поддерживает контекстный диалог с query transformation, и включает систему оценки качества через RAGAS метрики с интеграцией в LangSmith.

## Вариант задания

**Базовый**

## Используемые модели и провайдеры

### Для RAG системы

**LLM модели:**
- Основная модель для генерации ответов: `gpt-oss:20b` (локальная модель через Ollama)
- Модель для query transformation: `gpt-oss:20b` (локальная модель через Ollama)

**Embeddings:**
- Модель для создания эмбеддингов документов: `aroxima/multilingual-e5-large-instruct:latest` (локальная модель через Ollama)

**Провайдеры:**
- Используется локальный провайдер: **Ollama**
- Настройка через переменные окружения: `OLLAMA_BASE_URL=http://localhost:11434`, `LLM_PROVIDER=ollama`, `EMBEDDING_PROVIDER=ollama`

### Для RAGAS evaluation

**LLM модель:**
- `gpt-oss:20b` (локальная модель через Ollama)
- Провайдер: `ollama`

**Embeddings:**
- `aroxima/multilingual-e5-large-instruct:latest` (локальная модель через Ollama)
- Провайдер: `ollama`

*Примечание: Для единообразной оценки качества используются те же локальные модели, что и в основной RAG системе.*

## Создание и загрузка датасета

### Как создавали датасет

Датасет создавался **комбинацией двух методов**:

1. **Автоматический синтез из PDF документов:**
   - Загрузка PDF файлов из директории `data/`
   - Разбиение на чанки (chunk_size=500, chunk_overlap=50)
   - Равномерная выборка чанков из каждого PDF файла
   - Генерация вопросов и ответов через LLM на основе выбранных чанков
   - Используется промпт для создания реалистичных Q&A пар

2. **Загрузка готовых Q&A пар из JSON:**
   - Загрузка готовых вопросно-ответных пар из JSON файлов в директории `data/`
   - Случайная выборка примеров из каждого JSON файла
   - Сохранение метаданных (категория, URL источника)

Реализовано в модуле `src/dataset_synthesizer.py` с CLI интерфейсом через команды:
- `make dataset` - создание датасета
- `make dataset-upload` - загрузка в LangSmith

### Размер датасета

**Количество примеров: 6**

- 4 примера синтезированы из PDF документов (по 2 из каждого PDF)
- 2 примера загружены из JSON файла `sberbank_help_documents.json`

### Скриншот страницы датасета в LangSmith

![Скриншот датасета в LangSmith](screenshots/Dataset.png)

### Примеры Q&A пар из датасета

**Пример 1 (синтезирован из PDF):**
```
Вопрос: Кто является сторонами договора по потребительскому кредиту, согласно Общим условиям предоставления, обслуживания и погашения кредитов для физических лиц?

Ответ: Кредитор и Заемщик или Созаемщики.

Источник: ouk_potrebitelskiy_kredit_lph.pdf, стр. 0
```

**Пример 2 (из JSON):**
```
Вопрос: Изменение доли основного долга в обязательном платеже

Ответ: По договору в состав обязательного платежа включается до 10% от основного долга по кредитной карте (но не менее 150 рублей). Доля основного долга в обязательном платеже ежемесячно определяется банком индивидуально для каждого клиента...

Источник: sberbank_help_documents.json
Категория: Вопросы о кредитных картах
```

## Оценка качества через RAGAS

### Используемые метрики

Система использует **6 метрик RAGAS** для оценки качества RAG системы:

1. **Faithfulness (Обоснованность)** - проверяет, что ответ не содержит галлюцинаций и основан только на retrieved документах
2. **Answer Relevancy (Релевантность ответа)** - оценивает, насколько ответ релевантен заданному вопросу
3. **Answer Correctness (Правильность ответа)** - сравнивает ответ с ground truth эталоном
4. **Answer Similarity (Похожесть на эталон)** - вычисляет семантическую похожесть ответа на эталон через embeddings
5. **Context Recall (Полнота контекста)** - проверяет, содержат ли retrieved документы всю необходимую информацию для правильного ответа
6. **Context Precision (Точность поиска)** - оценивает, насколько retrieved документы релевантны вопросу

### Процесс evaluation

1. **Запуск эксперимента в LangSmith** - выполнение RAG цепочки для всех примеров датасета с `blocking=False`
2. **RAGAS batch evaluation** - вычисление всех метрик для собранных данных
3. **Загрузка feedback в LangSmith** - сохранение метрик как feedback к соответствующим runs

Реализовано в модуле `src/evaluation.py`, доступно через команду `/evaluate_dataset` в Telegram боте.

## Выводы

### Главные инсайты о качестве RAG системы

Результаты evaluation на датасете из 6 примеров показали следующее качество RAG системы:

**Метрики качества:**

- 🟢 **Обоснованность (Faithfulness)**: 0.864 — отличный результат
  - Система практически не генерирует галлюцинации, ответы основаны на retrieved документах

- 🟡 **Релевантность ответа (Answer Relevancy)**: 0.779 — хороший результат
  - Ответы в целом релевантны вопросам, но есть пространство для улучшения

- 🟡 **Правильность ответа (Answer Correctness)**: 0.607 — хороший результат
  - Ответы соответствуют эталону, но не всегда точно. Это самая низкая метрика, требует внимания

- 🟢 **Похожесть на эталон (Answer Similarity)**: 0.911 — отличный результат
  - Семантическая похожесть ответов на эталон очень высокая

- 🟢 **Полнота контекста (Context Recall)**: 1.000 — отличный результат
  - Retriever находит всю необходимую информацию для ответа на вопросы

- 🟢 **Точность поиска (Context Precision)**: 0.944 — отличный результат
  - Найденные документы очень релевантны вопросам

**Общая оценка:**

Система показывает **хорошее качество** с сильными сторонами в:
- Отсутствии галлюцинаций (Faithfulness: 0.864)
- Полноте и точности поиска (Context Recall: 1.000, Context Precision: 0.944)
- Семантической похожести ответов (Answer Similarity: 0.911)

**Области для улучшения:**
- Правильность ответа (Answer Correctness: 0.607) — самая низкая метрика, указывает на необходимость улучшения точности формулировок ответов
- Релевантность ответа (Answer Relevancy: 0.779) — можно улучшить через оптимизацию промптов или модели генерации

**Шкала оценки:** 🟢 0.8+ отлично | 🟡 0.6-0.8 хорошо | 🔴 <0.6 требует улучшений

---

*Отчёт создан автоматически на основе анализа кодовой базы проекта.*

