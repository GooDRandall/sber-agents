# Предложения инструментов для банковского бота

## Обзор
Сейчас бот использует только один инструмент - `rag_search` для поиска информации в документах. Ниже предложены дополнительные инструменты, которые расширят функциональность бота.

---

## 1. 🧮 Калькулятор кредита (`calculate_loan`)

**Назначение:** Расчет ежемесячного платежа, переплаты и графика платежей по кредиту.

**Параметры:**
- `amount` (float) - сумма кредита
- `interest_rate` (float) - процентная ставка годовых
- `term_months` (int) - срок кредита в месяцах
- `payment_type` (str) - тип платежа: "annuity" (аннуитетный) или "differentiated" (дифференцированный)

**Возвращает:** JSON с расчетом платежа, переплаты, общей суммы

**Пример использования:**
- "Рассчитай платеж по кредиту 500000 рублей на 5 лет под 12% годовых"
- "Сколько будет ежемесячный платеж при кредите 1 млн на 3 года под 15%?"

**Когда использовать:**
- Пользователь спрашивает про расчет платежей
- Нужно сравнить разные варианты кредита
- Вопросы о переплате

---

## 2. 💰 Калькулятор вклада (`calculate_deposit`)

**Назначение:** Расчет доходности вклада с учетом капитализации процентов.

**Параметры:**
- `amount` (float) - сумма вклада
- `interest_rate` (float) - процентная ставка годовых
- `term_months` (int) - срок вклада в месяцах
- `capitalization` (bool) - капитализация процентов (да/нет)

**Возвращает:** JSON с суммой в конце срока, начисленными процентами

**Пример использования:**
- "Сколько я получу, если положу 100000 рублей на год под 8% с капитализацией?"
- "Рассчитай доходность вклада 500000 на 6 месяцев под 7%"

**Когда использовать:**
- Вопросы о доходности вкладов
- Сравнение вариантов вкладов
- Расчет накоплений

---

## 3. 💱 Конвертер валют (`convert_currency`)

**Назначение:** Конвертация валют по актуальному курсу Сбербанка.

**Параметры:**
- `amount` (float) - сумма для конвертации
- `from_currency` (str) - исходная валюта (RUB, USD, EUR, etc.)
- `to_currency` (str) - целевая валюта

**Возвращает:** JSON с результатом конвертации и курсом

**Пример использования:**
- "Сколько будет 1000 долларов в рублях?"
- "Конвертируй 50000 рублей в евро"

**Когда использовать:**
- Вопросы о курсах валют
- Конвертация сумм
- Сравнение цен в разных валютах

**Примечание:** Для реального использования нужен API курсов валют. Для демо можно использовать фиксированные курсы или мок-данные.

---

## 4. 📍 Поиск отделений/банкоматов (`find_branches`)

**Назначение:** Поиск ближайших отделений или банкоматов Сбербанка.

**Параметры:**
- `location` (str) - адрес или название города/района
- `type` (str) - тип: "branch" (отделение) или "atm" (банкомат)
- `radius_km` (int, optional) - радиус поиска в км (по умолчанию 5)

**Возвращает:** JSON со списком отделений/банкоматов с адресами, часами работы, расстоянием

**Пример использования:**
- "Найди ближайшее отделение в Москве"
- "Где ближайший банкомат к метро Сокольники?"
- "Покажи отделения в Санкт-Петербурге"

**Когда использовать:**
- Вопросы о расположении отделений
- Поиск банкоматов
- Уточнение адресов и часов работы

**Примечание:** Для реального использования нужен API геолокации или база данных отделений. Для демо можно использовать мок-данные.

---

## 5. 📊 Расчет досрочного погашения (`calculate_early_repayment`)

**Назначение:** Расчет экономии при досрочном погашении кредита.

**Параметры:**
- `current_balance` (float) - текущий остаток долга
- `monthly_payment` (float) - текущий ежемесячный платеж
- `interest_rate` (float) - процентная ставка
- `remaining_months` (int) - оставшийся срок в месяцах
- `early_payment_amount` (float) - сумма досрочного погашения

**Возвращает:** JSON с новой суммой платежа, экономией, новым сроком

**Пример использования:**
- "Сколько я сэкономлю, если погашу досрочно 100000 рублей?"
- "Рассчитай новый платеж после досрочного погашения 50000"

**Когда использовать:**
- Вопросы о досрочном погашении
- Расчет экономии
- Планирование погашения кредита

---

## 6. 🔒 Блокировка карты (`block_card`)

**Назначение:** Симуляция блокировки карты в случае утери или кражи.

**Параметры:**
- `card_last_four` (str) - последние 4 цифры карты
- `reason` (str) - причина блокировки: "lost", "stolen", "suspicious"

**Возвращает:** JSON с подтверждением блокировки и инструкциями

**Пример использования:**
- "Заблокируй карту 1234, я её потерял"
- "Моя карта украдена, номер 5678"

**Когда использовать:**
- Сообщения о потере/краже карты
- Запросы на блокировку
- Экстренные ситуации

**Примечание:** В реальном боте это должно быть интегрировано с банковским API. Для демо - симуляция с логированием.

---

## 7. 📈 Проверка лимитов (`check_limits`)

**Назначение:** Проверка доступных лимитов по картам и кредитам (симуляция).

**Параметры:**
- `limit_type` (str) - тип лимита: "card_daily", "card_monthly", "credit_available", "cash_withdrawal"
- `card_last_four` (str, optional) - последние 4 цифры карты

**Возвращает:** JSON с информацией о лимитах

**Пример использования:**
- "Какой у меня дневной лимит по карте?"
- "Сколько я могу снять наличных сегодня?"
- "Какая доступная сумма по кредиту?"

**Когда использовать:**
- Вопросы о лимитах
- Проверка доступных средств
- Планирование операций

**Примечание:** Для реального использования нужна интеграция с банковским API. Для демо - мок-данные.

---

## 8. 📅 Расчет срока до цели (`calculate_savings_goal`)

**Назначение:** Расчет времени и суммы ежемесячных пополнений для достижения финансовой цели.

**Параметры:**
- `target_amount` (float) - целевая сумма
- `current_amount` (float) - текущая сумма
- `monthly_contribution` (float, optional) - ежемесячное пополнение
- `interest_rate` (float) - процентная ставка вклада
- `mode` (str) - режим: "time" (рассчитать срок) или "contribution" (рассчитать пополнение)

**Возвращает:** JSON с расчетом срока или суммы пополнения

**Пример использования:**
- "За сколько месяцев накоплю 500000, если откладываю 20000 в месяц под 7%?"
- "Сколько нужно откладывать ежемесячно, чтобы через 2 года накопить 1 млн?"

**Когда использовать:**
- Планирование накоплений
- Финансовое планирование
- Расчет целей

---

## 9. 🔍 Проверка статуса заявки (`check_application_status`)

**Назначение:** Проверка статуса заявки на кредит, вклад или карту (симуляция).

**Параметры:**
- `application_id` (str) - номер заявки
- `application_type` (str) - тип: "loan", "deposit", "card"

**Возвращает:** JSON со статусом заявки и дополнительной информацией

**Пример использования:**
- "Какой статус моей заявки на кредит номер 12345?"
- "Проверь статус заявки на карту"

**Когда использовать:**
- Вопросы о статусе заявок
- Отслеживание обработки
- Уточнение сроков

**Примечание:** Для реального использования нужна интеграция с системой обработки заявок. Для демо - мок-данные.

---

## 10. 💳 Сравнение продуктов (`compare_products`)

**Назначение:** Сравнение банковских продуктов (кредитов, вкладов, карт) по параметрам.

**Параметры:**
- `product_type` (str) - тип продукта: "loan", "deposit", "card"
- `criteria` (dict) - критерии сравнения (сумма, срок, и т.д.)

**Возвращает:** JSON с таблицей сравнения продуктов

**Пример использования:**
- "Сравни кредиты на 500000 рублей на 3 года"
- "Какие вклады выгоднее на сумму 1 млн на год?"

**Когда использовать:**
- Выбор между продуктами
- Сравнение условий
- Помощь в принятии решения

**Примечание:** Может использовать `rag_search` для получения информации о продуктах, затем форматировать сравнение.

---

## Рекомендации по приоритетам

### Высокий приоритет (рекомендуется реализовать первыми):
1. **Калькулятор кредита** - очень частый запрос клиентов
2. **Калькулятор вклада** - популярная функция
3. **Конвертер валют** - полезен для многих клиентов

### Средний приоритет:
4. **Расчет досрочного погашения** - важная функция для заемщиков
5. **Поиск отделений/банкоматов** - удобство для клиентов
6. **Расчет срока до цели** - полезно для финансового планирования

### Низкий приоритет (требуют интеграции с реальными системами):
7. **Блокировка карты** - нужна интеграция с банковским API
8. **Проверка лимитов** - нужна интеграция с банковским API
9. **Проверка статуса заявки** - нужна интеграция с системой заявок
10. **Сравнение продуктов** - можно реализовать через комбинацию `rag_search` и форматирования

---

## Технические замечания

1. **Моки vs Реальные API:**
   - Для демо/разработки можно использовать мок-данные
   - Для продакшена нужна интеграция с реальными банковскими системами
   - Некоторые инструменты (курсы валют) могут использовать публичные API

2. **Безопасность:**
   - Инструменты, работающие с реальными данными клиентов, требуют аутентификации
   - Блокировка карты должна быть защищена дополнительной верификацией

3. **Интеграция с агентом:**
   - Все инструменты должны быть декорированы `@tool` из LangChain
   - Добавляются в список `tools` в `agent.py`
   - Обновляется системный промпт для описания новых инструментов

